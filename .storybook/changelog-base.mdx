<Meta title="Changelog" />

# Changelog

## Upcoming in the next release:

#### Breaking changes

1. Prop `padding` value `xl` was changed from `2rem` to `1.5rem`. New prop value `2xl` was added that equals `2rem`. So to keep the same padding, replace `padding="xl"` with `padding="2xl"`

---

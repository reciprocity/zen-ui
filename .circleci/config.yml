defaults: &defaults
  docker:
    - image: circleci/node:10

version: 2
jobs:
  prepare:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          keys:
          - v1-npm-{{ checksum "yarn.lock" }}
          - v1-npm-
      - run: npm install
      - save_cache:
          paths:
            - node_modules
          key: v1-npm-{{ checksum "yarn.lock" }}
      - persist_to_workspace:
          root: .
          paths:
            - node_modules
  build:
    <<: *defaults
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: npm run build
      - persist_to_workspace:
          root: .
          paths:
            - build
  test:
    <<: *defaults
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: npm run test
  flow:
    <<: *defaults
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run: npm run flow
  deploy:
    resource_class: small
    docker:
      - image: quay.io/reciprocity/build-base:v0.6.7
        auth:
          username: $QUAY_USERNAME
          password: $QUAY_PASSWORD
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Upload build except index.html with cache headers
          command: aws s3 sync --cache-control 'max-age=604800' --exclude index.html --exclude remoteEntry.js build s3://zen-ui.zengrc.com --delete
      - run:
          name: Upload index with no cache
          command: aws s3 sync --cache-control 'no-cache' build s3://zen-ui.zengrc.com

workflows:
  version: 2
  test-build-deploy:
    jobs:
      - prepare
      - build:
          requires:
            - prepare
      - deploy:
          requires:
            - build
          filters:
            branches:
              only:
                - stencil
      - test:
          requires:
            - prepare
<Meta title="Changelog" />

# Changelog

## Upcoming in the next release:

Nothing so far...

---

## 0.1.7

25.3.2021<br/><br/>

#### Breaking changes

1. `zen-panel`: Padding is changed! Both header and content have default padding set.
1. `zen-panel`: Zen-text is no longer wrapping header. Common header should be `<zen-text slot="header">Item title</zen-text>`

#### PRs merged in this release

- `QUICK` Add lint:all and format:all commands
- `PLAT-1583` Add possibility to override avatar group colors
- `PLAT-1371` Panel - Add animations
- `PLAT-1601` Date picker is not closed properly if user type the date
- `PLAT-1543` Remove duplicated variable
- `QUICKFIX` re-enable lint-staged
- `PLAT-1494` Date picker width now can be changed
- `PLAT-1495` Dropdown receives focus but doesn't open
- `PLAT-1453` REQUEST: Datepicker default value
- `PLAT-844` Skeleton component
- `PLAT-1199` Fix Husky, lint-staged, ESLint && Prettier
- `PLAT-1542` Dropdown vrt test and fix for popover test
- `PLAT-1491` Reflect the change of the progress tracker active index
- `PLAT-1511` Update component events documentation
- `PLAT-1520` Add vrt and functional test for popover
- `PLAT-902` Implement notifications/alerts
- `PLAT-1461` Add indeterminate state Rebase
- `PLAT-1472` Remove negative margins from zen-space
- `PLAT-1485` Firefox Some stories are broken

---

## 0.1.6

12.3.2021<br/><br/>

#### Breaking changes

1. Removed component `zen-form-group`. Use component `zen-space` instead.
1. Renamed prop `padding` to `p` in components `zen-space`, `zen-icon`, `zen-avatar-details`.
1. `zen-icon` prop `padding` value `xl` was changed from `2rem` to `1.5rem`. New prop value `2xl` was added that equals `2rem`. So to keep the same padding, replace `padding="xl"` with `padding="2xl"`

#### PRs merged in this release

- `PLAT-1470` Zen-space padding should default to 'none'
- `QUICK-FIX` Fix textarea height on resizing
- `PLAT-1430` Add zen text improvements
- `QUICK-FIX` Move Panel under Layout group
- `PLAT-1462` Checkbox with intermediate state
- `PLAT-1426` Add resize, row, cols properties
- `Revert PLAT-1250` Zen space elements after first one in slot have margin left set."
- `PLAT-1338` Add avatar details component
- `PLAT-1174` Remove form-group component
- `QUICK-FIX` ZenPanel: Correctly use dynamic prefix
- `PLAT-1357` Default value for date format is not applied to the date picker
- `PLAT-1358` Date picker is missing a X icon to remove the date from field
- `QUICK-FIX` Pin dependencies
- `PLAT-1389` Dropdown - Scroll on keyboard selection

---

## 0.1.4

8.3.2021<br/><br/>

#### Breaking changes

1. Tooltip: props `showDelay` and `hideDelay` have been joined into prop `delay`. (Note that delay still supports shorthand for show and hide prop.)

---

## 0.1.3

19.2.2021<br/><br/>

#### Breaking changes

1. All zen components need to be prefixed.

---

## 0.1.2

17.2.2021<br/><br/>

#### Breaking changes

1. **zen-space**: `width: 100%` isn't default anymore. Add attr `stretch` to make it `width: 100%`. In most cases this should have no impact.

---

## 0.1.1

16.2.2021<br/><br/>

Bugfixes and new components.

---

## 0.1.0

1.2.2021<br/><br/>

#### Breaking changes

1. **Events prefixed by `zen-`** were changed **into native events**.<br/>Eg. `zen-change` is now `change`.<br/>This affects the following components:
   - **`zen-checkbox`**
   - **`zen-dropdown`**
   - **`zen-input`**
   - **`zen-textarea`**
   - **`zen-progress-tracker`**
1. **Removed components: `zen-label` and `zen-support-text`**. Use component `zen-text` with variant `label` or `support`
1. zen-spinner: **Prop `color` removed**. To set custom color: `<zen-spinner style="color: #f00">`
1. zen-button: **Prop `label` removed**. To set button label: `<zen-button>Click here</zen-button>`
1. zen-checkbox: **Prop `label` removed**. To set checkbox label: `<zen-checkbox>Checkbox label</zen-checkbox>`
1. **Component `zen-steps` was renamed to `zen-progress-tracker`**.

---

## 0.0.19

24.12.2020<br/><br/>

#### Features

1. Tooltip: New props `hasArrow`

### Bugfixes

1. Tooltip: Hide scroller if there's enough height

---

## 0.0.18

23.12.2020<br/><br/>

#### Features

1. Tooltip: new props `hideDelay`, `showDelay`
1. Tooltip: new prop `maxHeight` which allows you to create scrollable tooltips
1. Tooltip: tooltip will switch position if there's no room to show it

---

## 0.0.17

22.12.2020<br/><br/>

#### Breaking changes

1. Dropdown: slot `options` is now the default slot
1. Zen-option: slot `label` is now the default slot

#### Features

1. Dropdown: Added slot `placeholder`
1. Dropdown: Added prop `disabled`
1. Dropdown: Selected item is copied into the field, so the item in the field is styled correctly (has the same content as an original item - icons, padding & stuff)
1. Zen-option: Added prop `disabled`

#### Bugfixes

1. Dropdown: prevent closing dropdown on scrolling using the scrollbar
1. Dropdown: The height of the field should follow the height of `<zen-dropdown>` tag

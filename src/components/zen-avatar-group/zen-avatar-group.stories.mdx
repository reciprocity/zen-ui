import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-avatar-group');
const argTypes = getArgTypes(compData);

<Meta title="Icons/Avatar Group" component="zen-avatar-group" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-avatar-group ...="${spreadArgs(args)}"></zen-avatar-group>`;

export const StoryAvatarGroup = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <zen-avatar-group>
        <zen-avatar user-name="Mike Jackson" image-url="https://widgetwhats.com/app/uploads/2019/11/free-profile-photo-whatsapp-4.png"></zen-avatar>
        <zen-avatar user-name="Frank Lee" image-url="https://widgetwhats.com/app/uploads/2019/11/free-profile-photo-whatsapp-3.png"></zen-avatar>
        <zen-avatar user-name="Derek Franklin" image-url="https://widgetwhats.com/app/uploads/2019/11/free-profile-photo-whatsapp-3.png"></zen-avatar>
      </zen-avatar-group>
    </div>
  `;

# Grouped profile icons

Avatar group component

## Component previews

<br />

### 1. Avatar group variants

<Canvas>
  <Story name="StoryAvatarGroup">{StoryAvatarGroup.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-avatar-group'))}></docs-table>

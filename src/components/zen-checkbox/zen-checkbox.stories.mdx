import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import { eventHandles, action } from '../../../.storybook/helpers/custom-action';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-checkbox');
const argTypes = getArgTypes(compData);

<Meta title="Forms/Checkbox" component="zen-checkbox" argTypes={argTypes} />

export const StoryFormGroup = args =>
  html/*html*/ `
    <sb-zen-space vertical spacing="md">
      <sb-zen-text variant="label">Pizza toppings</sb-zen-text>
      <sb-zen-checkbox label="Pepperoni"></sb-zen-checkbox>
      <sb-zen-checkbox label="Black olives"></sb-zen-checkbox>
      <sb-zen-checkbox label="Veggies"></sb-zen-checkbox>
    </sb-zen-space>
  `;

export const StoryIndeterminate = args =>
  html/*html*/ `
    <sb-zen-space vertical spacing="md">
      <sb-zen-text variant="label">Books</sb-zen-text>
      <sb-zen-checkbox label="Clean Code" indeterminate></sb-zen-checkbox>
      <sb-zen-checkbox label="The Pragmatic Programmer" indeterminate></sb-zen-checkbox>
      <sb-zen-checkbox label="Learning Rust" checked></sb-zen-checkbox>
      <sb-zen-checkbox label="Effective Java"></sb-zen-checkbox>
    </sb-zen-space>
  `;

export const StoryWithControls = args => html/*html*/ `
  <sb-zen-checkbox id="story-with-ctrls-element" ...="${spreadArgs(args, argTypes)}"></sb-zen-checkbox>
  ${action('#story-with-ctrls-element', ['change', 'click'])}
`;

# Checkbox

Simple checkbox component with label and required properties

## Component previews

<br />

### 1. In a form group

<Canvas>
  <Story name="StoryFormGroup">{StoryFormGroup.bind({})}</Story>
</Canvas>

### 2. Indeterminate state

<Canvas>
  <Story name="StoryIndeterminate">{StoryIndeterminate.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      label: 'I have read and agree to the Terms and Conditions',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-checkbox'))}></docs-table>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import Readme from './readme.md';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-checkbox');
const argTypes = getArgTypes(compData);

<Meta title="Forms/Checkbox" component="zen-checkbox" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ `<zen-checkbox ...="${spreadArgs(args)}"></zen-checkbox>`;

# Checkbox

Simple checkbox component with label and required properties

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      label: 'I have read and agree to the Terms and Conditions',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-checkbox'))}></docs-table>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import Readme from './readme.md';
import { eventHandles, action } from '../../../.storybook/helpers/custom-action';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-button');
const argTypes = getArgTypes(compData);

<Meta title="Buttons/Button" component="zen-button" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ `
  <zen-button id="button1" ...="${spreadArgs(args)}"> Button 1 </zen-button>
  ${action('#button1', eventHandles(['click']))}
`;

export const StorySimple = () => html/*html*/ ` <zen-button variant="primary"></zen-button> `;

export const StoryLoading = () => html/*html*/ ` <zen-button loading></zen-button> `;

export const StoryDisabled = () => html/*html*/ ` <zen-button disabled></zen-button> `;

export const StoryWithLeadingSlot = () => html/*html*/ ` <zen-button> <zen-spinner slot="leadingIcon" /></zen-button> `;

export const StoryWithTrailingSlot = () =>
  html/*html*/ ` <zen-button><zen-spinner slot="trailingIcon" /></zen-button> `;

export const StoryWithLeadingAndTrailingSlots = () =>
  html/*html*/ `
    <zen-button id="button6">
      <zen-spinner slot="leadingIcon"></zen-spinner>
      Leading and Trailing Slots
      <zen-spinner slot="trailingIcon"></zen-spinner>
    </zen-button>
  `;

# Button

Button component with slots capabilities

## Component previews

<br />

### 1. Default button

- Important point for this story

<Canvas>
  <Story name="StorySimple">{StorySimple.bind({})}</Story>
</Canvas>

### 2. Default loading button

- Important point for this story

<Canvas>
  <Story name="StoryLoading">{StoryLoading.bind({})}</Story>
</Canvas>

### 3. Default disabled button

- Important point for this story

<Canvas>
  <Story name="StoryDisabled">{StoryDisabled.bind({})}</Story>
</Canvas>

### 4. Default button with leading slot fulfilled

- Important point for this story

<Canvas>
  <Story name="StoryWithLeadingSlot">{StoryWithLeadingSlot.bind({})}</Story>
</Canvas>

### 5. Default button with trailing slot fulfilled

- Important point for this story

<Canvas>
  <Story name="StoryWithTrailingSlot">{StoryWithTrailingSlot.bind({})}</Story>
</Canvas>

### 6. Default button with leading and trailing slots fulfilled

- Important point for this story

<Canvas>
  <Story name="StoryWithLeadingAndTrailingSlots">{StoryWithLeadingAndTrailingSlots.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      // override some default values:
      variant: 'primary',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-button'))}></docs-table>

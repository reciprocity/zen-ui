import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-button');
const argTypes = getArgTypes(compData);

import { litHtmlIcon, styles } from '../helpers/fa-icons';
import { faUser, faChevronDown } from '@fortawesome/pro-light-svg-icons';

<Meta title="Buttons/Button" component="zen-button" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-button ...="${spreadArgs(args)}">Button</zen-button>`;

export const StorySimple = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button variant="primary">Button</zen-button>
        <span style="margin-top: 0.5rem;">Primary</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button variant="secondary">Button</zen-button>
        <span style="margin-top: 0.5rem;">Secondary</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button variant="tertiary">Button</zen-button>
        <span style="margin-top: 0.5rem;">Tertiary</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button variant="positive">Button</zen-button>
        <span style="margin-top: 0.5rem;">Positive</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button variant="destructive">Button</zen-button>
        <span style="margin-top: 0.5rem;">Destructive</span>
      </div>
    </div>
  `;

export const StoryLoading = () => html/*html*/ ` <zen-button loading>Button</zen-button> `;

export const StoryDisabled = () => html/*html*/ ` <zen-button disabled>Button</zen-button> `;

export const StoryWithIcons = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button>
          <i slot="leadingIcon" style="width: 0.625rem;" .innerHTML="${litHtmlIcon(faUser)}"></i>
          Leading Slot
        </zen-button>
        <span style="margin-top: 0.5rem;">Leading Slot</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button>
          Trailing Slot
          <i slot="trailingIcon" style="width: 0.625rem;" .innerHTML="${litHtmlIcon(faChevronDown)}"></i>
        </zen-button>
        <span style="margin-top: 0.5rem;">Trailing Slot</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button>
          <i slot="leadingIcon" style="width: 0.625rem;" .innerHTML="${litHtmlIcon(faUser)}"></i>
          Leading and Trailing Slots
          <i slot="trailingIcon" style="width: 0.625rem;" .innerHTML="${litHtmlIcon(faChevronDown)}"></i>
        </zen-button>
        <span style="margin-top: 0.5rem;">Leading and Trailing Slots</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-button>
          <i slot="leadingIcon" style="width: 0.625rem;" .innerHTML="${litHtmlIcon(faUser)}"></i>
        </zen-button>
        <span style="margin-top: 0.5rem;">Just icon</span>
      </div>
    </div>
  `;

# Button

Button component with slots capabilities

## Component previews

<br />

### 1. Button variants

<Canvas>
  <Story name="StorySimple">{StorySimple.bind({})}</Story>
</Canvas>

### 2. Loading button

<Canvas>
  <Story name="StoryLoading">{StoryLoading.bind({})}</Story>
</Canvas>

### 3. Disabled button

<Canvas>
  <Story name="StoryDisabled">{StoryDisabled.bind({})}</Story>
</Canvas>

### 4. With Icons

- Apply leadingIcon and trailingIcon values for the slot attributes in your inner custom HTML

<Canvas>
  <Story name="StoryWithLeadingSlot">{StoryWithIcons.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      // override some default values:
      variant: 'primary',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-button'))}></docs-table>

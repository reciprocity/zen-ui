import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-toggle');
const argTypes = getArgTypes(compData);

<Meta title="Forms/Toggle" component="zen-toggle" argTypes={argTypes} />

export const StoryWithVariants = args =>
  html/*html*/ `
    <div>
      <sb-zen-space vertical spacing="md" padding="sm none">
        <sb-zen-text variant="label">Unchecked</sb-zen-text>
        <sb-zen-toggle></sb-zen-toggle>
      </sb-zen-space>
      <sb-zen-space vertical spacing="md" padding="sm none">
        <sb-zen-text variant="label">Checked</sb-zen-text>
        <sb-zen-toggle checked></sb-zen-toggle>
      </sb-zen-space>
      <sb-zen-space vertical spacing="md" padding="sm none">
        <sb-zen-text variant="label">Disabled unchecked</sb-zen-text>
        <sb-zen-toggle disabled></sb-zen-toggle>
      </sb-zen-space>
      <sb-zen-space vertical spacing="md" padding="sm none">
        <sb-zen-text variant="label">Disabled checked</sb-zen-text>
        <sb-zen-toggle disabled checked></sb-zen-toggle>
      </sb-zen-space>
    </div>
  `;

export const StoryWithControls = args =>
  html/*html*/ ` <sb-zen-toggle ...="${spreadArgs(args, argTypes)}"></sb-zen-toggle> `;

# Toggle

Simple toggle component

<br />

## Component previews

<br />

### Toggle variants

<Canvas>
  <Story
    name="variants"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithVariants.bind({})}
  </Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<sb-docs-table docs={JSON.stringify(getComponentData('zen-toggle'))}></sb-docs-table>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import avatar from '../../img/avatar.jpg';
import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-avatar-icon');
const argTypes = getArgTypes(compData);
argTypes['background'].control = 'color';
argTypes['color'].control = 'color';

<Meta title="Graphics/Avatar/Avatar Icon" component="zen-avatar-icon" argTypes={argTypes} />

export const StoryWithControls = args =>
  html/*html*/ ` <sb-zen-avatar-icon ...="${spreadArgs(args, argTypes)}"></sb-zen-avatar-icon>`;

export const StoryAvatarVariants = () =>
  html/*html*/ `
    <div style="font-size:12px; display: flex; justify-content: space-evenly;">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <sb-zen-avatar-icon user-name="Mike Anderson"></sb-zen-avatar>
        <span>Initials from user name</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <sb-zen-avatar-icon email="mike.anderson@reciprocity.com"></sb-zen-avatar-icon>
        <span>Initials from email</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <sb-zen-avatar-icon image-url="${avatar}"></sb-zen-avatar-icon>
        <span>Content from image url</span>
      </div>
    </div>
  `;

export const StoryAvatarSizes = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <sb-zen-avatar-icon user-name="Niki Franklin" background="#FFEED2" color="#A26900" size="sm"></sb-zen-avatar-icon>
      <sb-zen-avatar-icon user-name="Mike Anderson" background="#FADBFF" color="#643A6B" size="md"></sb-zen-avatar-icon>
      <sb-zen-avatar-icon user-name="Frank Money" color="" size="lg"></sb-zen-avatar-icon>
    </div>
  `;

# Profile icon

Avatar icon component

## Component previews

<br />

### 1. Avatar icon variants

<Canvas>
  <Story name="StoryAvatarVariants">{StoryAvatarVariants.bind({})}</Story>
</Canvas>

### 2. Avatar icon sizes

<Canvas>
  <Story name="StoryAvatarSizes">{StoryAvatarSizes.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      userName: 'Mike Anderson',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<sb-docs-table docs={JSON.stringify(getComponentData('zen-avatar-icon'))}></sb-docs-table>

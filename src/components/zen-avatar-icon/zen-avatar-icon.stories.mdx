import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import avatar from '../../img/avatar.jpg';
import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-avatar-icon');
const argTypes = getArgTypes(compData);
argTypes['background'].control = 'color';
argTypes['color'].control = 'color';

<Meta title="Icons/Avatar Icon" component="zen-avatar-icon" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-avatar-icon ...="${spreadArgs(args)}"></zen-avatar-icon>`;

export const StoryAvatarVariants = () =>
  html/*html*/ `
    <div style="font-size:12px; display: flex; justify-content: space-evenly;">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-avatar-icon user-name="Mike Anderson"></zen-avatar>
        <span>Initials from user name</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-avatar-icon email="mike.anderson@reciprocity.com"></zen-avatar-icon>
        <span>Initials from email</span>
      </div>
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-avatar-icon image-url="${avatar}"></zen-avatar-icon>
        <span>Content from image url</span>
      </div>
    </div>
  `;

export const StoryAvatarSizes = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <zen-avatar-icon user-name="Niki Franklin" background="#FFEED2" color="#A26900" size="sm"></zen-avatar-icon>
      <zen-avatar-icon user-name="Mike Anderson" background="#FADBFF" color="#643A6B" size="md"></zen-avatar-icon>
      <zen-avatar-icon user-name="Frank Money" color="" size="lg"></zen-avatar-icon>
    </div>
  `;

# Profile icon

Avatar icon component

## Component previews

<br />

### 1. Avatar icon variants

<Canvas>
  <Story name="StoryAvatarVariants">{StoryAvatarVariants.bind({})}</Story>
</Canvas>

### 2. Avatar icon sizes

<Canvas>
  <Story name="StoryAvatarSizes">{StoryAvatarSizes.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      userName: 'Mike Anderson',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-avatar-icon'))}></docs-table>

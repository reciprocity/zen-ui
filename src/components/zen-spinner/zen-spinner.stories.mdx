import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-spinner');
const argTypes = getArgTypes(compData);

<Meta title="Icons/Spinner" component="zen-spinner" argTypes={argTypes} />

export const StoryWithControls = args => {
  return html/*html*/ ` <sb-zen-spinner ...="${spreadArgs(args, argTypes)}"></sb-zen-spinner> `;
};

export const StoryColors = () => {
  return html/*html*/ `
    <div style="display: flex">
      <sb-zen-spinner style="color: blue"></sb-zen-spinner>
      <sb-zen-spinner class="ml-12" style="color: #34835b"></sb-zen-spinner>
      <sb-zen-spinner class="ml-12" style="color: rgb(219, 52, 68)"></sb-zen-spinner>
      <sb-zen-spinner class="ml-12" style="color: hsl(38, 100%, 56%)"></sb-zen-spinner>
    </div>
  `;
};

# Spinner

Spinner icon that represents loading

## Component previews

<br />

### 1. Custom colored spinners

- Just set `color` css property to spinner element:

<Canvas>
  <Story name="button">{StoryColors.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-spinner'))}></docs-table>

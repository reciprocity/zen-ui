import { html } from 'lit-html';
import Readme from './readme.md';
import { Meta, Description, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { getArgTypesAndArgs, spreadArgs } from '../../../.storybook/helpers/argTypes';
import { waitForElement } from '../../../.storybook/helpers/utils';
import { faChevronDoubleRight } from '@fortawesome/pro-light-svg-icons';
const { argTypes, args } = getArgTypesAndArgs('zen-drawer');

<Meta title="Layout/Drawer" component="zen-drawer" argTypes={argTypes} />

export const Template = args => {
  waitForElement('#zen-icon-element', element => {
    element.icon = faChevronDoubleRight;
  });
  function toggleDrawer() {
    const drawer = document.querySelector('#drawer1');
    drawer.opened = !drawer.opened;
  }
  return html/*html*/ `
    <sb-zen-space
      padding="none"
      spacing="none"
      horizontal-align="center"
      style="border: 1px solid lightgrey; position:relative; height: 400px"
    >
      <sb-zen-button @click="${toggleDrawer}">Open Drawer</sb-zen-button>
      <sb-zen-drawer id="drawer1" ...="${spreadArgs(args, argTypes)}">
        <sb-zen-space vertical="true" padding="lg" space="lg">
          <sb-zen-text variant="label" size="sm">Title</sb-zen-text>
          <sb-zen-text size="sm">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry.
          </sb-zen-text>
          <sb-zen-text variant="label" size="sm">Description</sb-zen-text>
          <sb-zen-text size="sm">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum is simply dummy text
            of the printing and typesetting industry. Lorem Ipsum is simply dummy text of the printing and typesetting
            industry. Lorem Ipsum is simply dummy text of the printing and typesetting industry.
          </sb-zen-text>
        </sb-zen-space>
      </sb-zen-drawer>
    </sb-zen-space>
  `;
};

export const TemplateVariants = args => {
  waitForElement('#zen-icon-element', element => {
    element.icon = faChevronDoubleRight;
  });
  function toggleDrawer() {
    const drawer = document.querySelector('#drawer1');
    drawer.opened = !drawer.opened;
  }
  return html/*html*/ `
    <sb-zen-space
      padding="none"
      spacing="none"
      horizontal-align="center"
      style="border: 1px solid lightgrey; position:relative; height: 400px"
    >
      <sb-zen-button @click="${toggleDrawer}">Open Drawer</sb-zen-button>
      <sb-zen-drawer id="drawer1" ...="${spreadArgs(args, argTypes)}">
        <sb-zen-space vertical="true" padding="lg" space="lg">
          <sb-zen-text variant="label" size="sm">Title</sb-zen-text>
          <sb-zen-text size="sm">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry.
          </sb-zen-text>
          <sb-zen-text variant="label" size="sm">Description</sb-zen-text>
          <sb-zen-text size="sm">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry.
          </sb-zen-text>
        </sb-zen-space>
      </sb-zen-drawer>
    </sb-zen-space>
  `;
};

# Zen Drawer

Drawer that opens on an open event.

## Component previews

<br />

#### 1. Different variants:

Use prop `variant` to style the tooltip.

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...args,
      position: 'right',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<div class="stencildoc" />
<Description>{Readme}</Description>

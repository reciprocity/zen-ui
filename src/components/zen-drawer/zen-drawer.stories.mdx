import { html } from 'lit-html';
import Readme from './readme.md';
import { Meta, Description, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { getArgTypesAndArgs, spreadArgs } from '../../../.storybook/helpers/argTypes'; import {waitForElement} from "../../../.storybook/helpers/utils"; import {faChevronDoubleRight} from "@fortawesome/pro-light-svg-icons";
const { argTypes, args } = getArgTypesAndArgs('zen-drawer');

<Meta title="In progress/Drawer" component="zen-drawer" argTypes={argTypes} />

export const Template = args => {
  waitForElement('#zen-icon-element', element => {
    element.icon = args.icon;
  });
  function showDrawer() {
    document.querySelector('#drawer1').show = false;
    document.querySelector('#drawer1').show = true;
  }
  function hideDrawer() {
    document.querySelector('#drawer1').show = true;
    document.querySelector('#drawer1').show = false;
  }
  return html/*html*/ `
    <div style="height: 200px;">
      <sb-zen-button @click="${showDrawer}">Open Drawer</sb-zen-button>
      <sb-zen-drawer id="drawer1"  ...="${spreadArgs(args, argTypes)}">
        <sb-zen-button variant="tertiary" @click="${hideDrawer}">
             <sb-zen-icon size="sm" id="zen-icon-element"></sb-zen-icon>
        </sb-zen-button>
        <sb-zen-space vertical="true" padding="lg" space="lg">
          <sb-zen-text variant="label" size="sm">Title</sb-zen-text>
          <sb-zen-text size="sm">Lorem Ipsum is simply dummy text of the printing and typesetting industry.</sb-zen-text>
          <sb-zen-text variant="label" size="sm">Description</sb-zen-text>
          <sb-zen-text size="sm">Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum is simply dummy text of the printing and typesetting industry.
          </sb-zen-text>
          </sb-zen-text>
        </sb-zen-space>
      </sb-zen-drawer>
    </div>
  `;
};

# Zen Drawer

Drawer that opens on an open event.

## Component previews

<br />

#### 1. Different variants:

Use prop `variant` to style the tooltip.


## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...args,
       icon: faChevronDoubleRight,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<div class="stencildoc" />
<Description>{Readme}</Description>

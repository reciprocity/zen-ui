import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-notification');
const argTypes = getArgTypes(compData);

<Meta title="Notifications/Notification" component="zen-notification" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-notification ...="${spreadArgs(args, argTypes)}" /> `;

export const StoryVariants = () => html/*html*/ ` <div style="padding-bottom: 20px">
    <zen-notification
      variant="success"
      heading="Success"
      message="Settings successfully saved!"
      dismiss-duration="none"
      dismiss="true"
    />
  </div>
  <div style="padding-bottom: 20px">
    <zen-notification
      variant="info"
      heading="Info"
      message="You have 6 items that need your attention!"
      dismiss-duration="none"
      dismiss="true"
    />
  </div>
  <div style="padding-bottom: 20px">
    <zen-notification
      variant="warning"
      heading="Warning"
      message="Proceed with caution. Any changes will
  lead to the recalculation of the risk ratings and scores accross the entire risk register."
      dismiss-duration="none"
      dismiss="true"
    />
  </div>
  <div>
    <zen-notification
      variant="error"
      heading="Error"
      message="Oops, something went wrong."
      dismiss-duration="none"
      dismiss="true"
    />
  </div>`;

# Notification

Toast notifications. Can be dismissable or permanent.
Use when you need to notify user, but don't need his direct response.

## Component previews

<br />

### 1. Variants

- There are different `variants` of notification

<Canvas>
  <Story name="button">{StoryVariants.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      // override some default values:
      dismissDuration: 'none',
      heading: 'Success',
      message: 'Settings successfully saved!',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-notification'))}></docs-table>

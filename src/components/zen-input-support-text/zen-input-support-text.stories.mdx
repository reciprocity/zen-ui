import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-input-support-text');
const argTypes = getArgTypes(compData);

<Meta title="Forms/一Fragments/Input Support Text" component="zen-input-support-text" argTypes={argTypes} />

export const StoryWithControls = args =>
  html/*html*/ `<zen-input-support-text ...="${spreadArgs(args)}"></zen-input-support-text>`;

export const StorySimple = () =>
  html/*html*/ `
    <zen-form-group>
      <zen-input placeholder="Username"></zen-input>
      <zen-input-support-text text="The username should be unique"></zen-input-support-text>
    </zen-form-group>
  `;

# Input Support Text

Simple input support text component that can be used to show useful information about the input

## Component previews

<br />

### 1. Input with Support Text

<Canvas>
  <Story name="StorySimple">{StorySimple.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      text: 'The password must have more than 8 characters',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-input-support-text'))}></docs-table>

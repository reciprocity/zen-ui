import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import avatar from '../../img/avatar.jpg';
import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-avatar');
const argTypes = getArgTypes(compData);
argTypes['background'].control = 'color';
argTypes['color'].control = 'color';

<Meta title="Icons/Avatar" component="zen-avatar" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-avatar ...="${spreadArgs(args)}"></zen-avatar>`;

export const StoryAvatarVariants = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <zen-avatar label="MP"></zen-avatar>
      <zen-avatar image-url="${avatar}" label="MP"></zen-avatar>
    </div>
  `;

export const StoryAvatarSizes = () =>
  html/*html*/ `
    <div style="display: flex; justify-content: space-evenly;">
      <zen-avatar label="MP" color="" size="large"></zen-avatar>
      <zen-avatar label="VP" background="#FADBFF" color="#643A6B" size="medium"></zen-avatar>
    </div>
  `;

# Profile icon

Avatar component

## Component previews

<br />

### 1. Avatar variants

<Canvas>
  <Story name="StoryAvatarVariants">{StoryAvatarVariants.bind({})}</Story>
</Canvas>

### 2. Avatar sizes

<Canvas>
  <Story name="StoryAvatarSizes">{StoryAvatarSizes.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      label: 'MP',
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-avatar'))}></docs-table>

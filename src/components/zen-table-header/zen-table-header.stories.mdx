import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-table-header');
const argTypes = getArgTypes(compData);
argTypes['$indeterminate'].table.disable = true;

<Meta title="Lists/Table/Header" component="zen-table-header" argTypes={argTypes} />

export const StoryHeader = () => {
  return html/*html*/ `
    <sb-zen-table>
      <sb-zen-table-header>
        <sb-zen-table-header-cell>Title 1</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 2</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 3</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 4</sb-zen-table-header-cell>
      </sb-zen-table-header>
    </sb-zen-table>
  `;
};

export const StoryHeaderSelectable = () => {
  return html/*html*/ `
    <sb-zen-table>
      <sb-zen-table-header selectable>
        <sb-zen-table-header-cell>Title 1</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 2</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 3</sb-zen-table-header-cell>
        <sb-zen-table-header-cell>Title 4</sb-zen-table-header-cell>
      </sb-zen-table-header>
      <sb-zen-table-row selectable>
        <sb-zen-table-cell>Row 1, Cell 1</sb-zen-table-cell>
        <sb-zen-table-cell>Row 1, Cell 2</sb-zen-table-cell>
        <sb-zen-table-cell>Row 1, Cell 3</sb-zen-table-cell>
        <sb-zen-table-cell>Row 1, Cell 4</sb-zen-table-cell>
      </sb-zen-table-row>
      <sb-zen-table-row selectable>
        <sb-zen-table-cell>Row 2, Cell 1</sb-zen-table-cell>
        <sb-zen-table-cell>Row 2, Cell 2</sb-zen-table-cell>
        <sb-zen-table-cell>Row 2, Cell 3</sb-zen-table-cell>
        <sb-zen-table-cell>Row 2, Cell 4</sb-zen-table-cell>
      </sb-zen-table-row>
    </sb-zen-table>
  `;
};

export const StoryWithControls = args => html/*html*/ `
  <sb-zen-table>
    <sb-zen-table-header ...="${spreadArgs(args, argTypes)}">
      <sb-zen-table-header-cell>Title 1</sb-zen-table-header-cell>
      <sb-zen-table-header-cell>Title 2</sb-zen-table-header-cell>
      <sb-zen-table-header-cell>Title 3</sb-zen-table-header-cell>
      <sb-zen-table-header-cell>Title 4</sb-zen-table-header-cell>
    </sb-zen-table-header>
  </sb-zen-table>
`;

# ZenTableHeader

Table header.

## Component previews

<br />

### 1. Single header

A fragment `ZenTableHeader`

<Canvas>
  <Story name="StoryHeader">{StoryHeader.bind({})}</Story>
</Canvas>

### 2. Header that can be selected

`ZenTableHeader` fragment component with selectable enabled.

<Canvas>
  <Story name="StoryHeaderSelectable">{StoryHeaderSelectable.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      selectable: true,
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<sb-docs-table docs={JSON.stringify(getComponentData('zen-table-header'))}></sb-docs-table>

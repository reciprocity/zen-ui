import { html } from 'lit-html';
import Readme from './readme.md';
import { Meta, Description, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';

import icon from '../../img/reci-icon-g.png';
import { faCheck } from '@fortawesome/pro-light-svg-icons';
import { litHtmlIcon, styles } from '../helpers/fa-icons';

import { getArgTypesAndArgs, spreadArgs } from '../../../.storybook/helpers/argTypes';
const { argTypes, args } = getArgTypesAndArgs('zen-tooltip');

<Meta title="Notifications/Tooltip" component="zen-tooltip" argTypes={argTypes} />

export const Template = args => {
  return html/*html*/ `
    <div style="text-align: center" class="my-48">
      <zen-button style="display:inline-block" label="Apply changes" variant="secondary"></zen-button>
      <zen-tooltip ...="${spreadArgs(args)}"></zen-tooltip>
    </div>
  `;
};

export const TemplateSlot = () => {
  return html/*html*/ `
    <style>
      .row {
        display: flex;
      }
      .column {
        flex-direction: column;
        text-align: left;
        margin-right: 0.7rem;
      }
      .tooltip-title {
        font-size: 0.875rem;
        margin-bottom: 0.2rem;
        font-weight: bold;
      }
      .tooltip-code {
        font-size: 0.75rem;
        margin-bottom: 0.2rem;
      }
      .tooltip-role {
        color: #868e96;
        font-size: 0.625rem;
        font-weight: bold;
        line-height: 1rem;
        text-transform: uppercase;
        margin: 0;
      }
      .profile {
        min-height: 2rem;
        min-width: 2rem;
        max-height: 2rem;
        max-width: 2rem;
        border-radius: 50%;
        position: relative;
        cursor: pointer;
        background: #ffdaff;
        text-align: center;
        line-height: 2rem;
        color: #b875c4;
      }
    </style>
    <div class="mt-56" style="cursor:pointer; text-align: center">
      <span>kim.anderson@reciprocity.com</span>
      <zen-tooltip
        style="max-width: fit-content;"
        always-visible="true"
        position="top"
        variant="light"
        label="Override text"
      >
        <div class="row">
          <div class="column profile">KA</div>
          <div class="column">
            <span class="tooltip-title">Kim Anderson</span>
            <p class="tooltip-code">kim.anderson@reciprocitylabs.com</p>
            <p class="tooltip-role">administrator</p>
          </div>
        </div>
      </zen-tooltip>
    </div>
  `;
};

export const TemplateVariants = () => {
  return html/*html*/ `
    <style>
      .container {
        display: flex;
        justify-content: space-between;
        padding-top: 7rem;
        padding-left: 7rem;
        padding-right: 7rem;
        text-align: center;
      }
    </style>
    <div class="container">
      <div>
        <zen-button style="display: block" label="Edit" variant="primary"></zen-button style="display: block">
        <zen-tooltip
          always-visible="true"
          position="top"
          variant="dark"
          label="The Manager can edit and create the PCI-DSS."
        ></zen-tooltip>
      </div>
      <div>
        <zen-button label="Info" variant="secondary"></zen-button>
        <zen-tooltip
          always-visible="true"
          position="top"
          variant="light"
          label="This is a information tooltip."
        ></zen-tooltip>
      </div>
      <div>
        <zen-button label="Delete" variant="destructive"></zen-button>
        <zen-tooltip
          always-visible="true"
          position="top"
          variant="error"
          label="Are you sure you want to delete the object!"
        ></zen-tooltip>
      </div>
    </div>
  `;
};

export const Scrollable = () => {
  return html/*html*/ `
    <style>
      .host {
        max-width: fit-content;
      }
      .row {
        display: flex;
      }
      .column {
        flex-direction: column;
        text-align: left;
        margin-right: 0.7rem;
      }
      .tooltip-title {
        font-size: 0.875rem;
        margin-bottom: 0.2rem;
        font-weight: bold;
      }
      .tooltip-code {
        font-size: 0.75rem;
        margin-bottom: 0.2rem;
      }
      .tooltip-role {
        color: #868e96;
        font-size: 0.625rem;
        font-weight: bold;
        line-height: 1rem;
        text-transform: uppercase;
        margin: 0;
      }
      .profile {
        min-height: 2rem;
        min-width: 2rem;
        max-height: 2rem;
        max-width: 2rem;
        border-radius: 50%;
        position: relative;
        cursor: pointer;
        background: #e7e7e7;
        text-align: center;
        line-height: 2rem;
        color: #b875c4;
      }
    </style>
    <div class="mt-64" style="cursor:pointer; text-align: center">
      <span>Multiple users selected</span>
      <zen-tooltip class="host" position="top" variant="light" label="Override text" max-height="4rem">
        <div>
          <div class="row py-8">
            <div class="column profile" style="background: #ffdaff">KA</div>
            <div class="column">
              <span class="tooltip-title">Kim Anderson</span>
              <p class="tooltip-code">kim.anderson@reciprocitylabs.com</p>
              <p class="tooltip-role">administrator</p>
            </div>
          </div>
          <div class="row py-8">
            <div class="column profile" style="background: #ffdaff">KA</div>
            <div class="column">
              <span class="tooltip-title">John Herschel Glenn Jr.</span>
              <p class="tooltip-code">john.glenn@reciprocitylabs.com</p>
              <p class="tooltip-role">administrator</p>
            </div>
          </div>
          <div class="row py-8">
            <div class="column profile" style="background: #ffdaff">KA</div>
            <div class="column">
              <span class="tooltip-title">Buzz Aldrin</span>
              <p class="tooltip-code">buzz.aldrin@reciprocitylabs.com</p>
              <p class="tooltip-role">reader</p>
            </div>
          </div>
        </div>
      </zen-tooltip>
    </div>
  `;
};

# Zen Tooltip

Tooltip bubbles that usually appears on element hover

## Component previews
<br/>

#### 1. Different variants:

Use prop `variant` to style the tooltip.

<Canvas>
  <Story name="variant">{TemplateVariants.bind({})}</Story>
</Canvas>

#### 2. Custom content:

Wrap some html content with `<zen-tooltip>` and it will appear inside the tooltip.

<Canvas>
  <Story name="slot">{TemplateSlot.bind({})}</Story>
</Canvas>

#### 3. Scrollable tooltip:

Limit tooltip height by setting `max-height` ('4rem' in example below).

<Canvas>
  <Story name="scrollable">{Scrollable.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...args,
      position: 'top',
      variant: 'dark',
      label: 'The Manager can edit and create in the context of the PCI-DSS framework.',
      offset: 10,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<div class="stencildoc" />
<Description>{Readme}</Description>

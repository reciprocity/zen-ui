import { html } from 'lit-html';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { getArgTypesAndArgs, getComponentData, getDefaultArgs, spreadArgs } from '../../../.storybook/helpers/argTypes';
const { argTypes } = getArgTypesAndArgs('zen-sortable-item');

<Meta title="Lists/Sortable/Sortable Item" component="zen-sortable-item" argTypes={argTypes} />

export const StoryCustomContent = args => {
  return html/*html*/ `
    <sb-zen-sortable-item>
      <sb-zen-text>Item</sb-zen-text>
      <sb-zen-toggle></sb-zen-toggle>
    </sb-zen-sortable-item>
  `;
};

export const StoryWithControls = args => {
  return html/*html*/ `
    <sb-zen-sortable-item ...="${spreadArgs(args, argTypes)}">
      <sb-zen-text>Item</sb-zen-text>
    </sb-zen-sortable-item>
  `;
};

# Zen Sortable Item

A sortable fragment for use in zen-sortable component.

<br />

## Component previews

`ZenSortableItem` A draggable icon followed by default slot content.

<Canvas>
  <Story name="CustomContent">{StoryCustomContent.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<sb-docs-table docs={JSON.stringify(getComponentData('zen-sortable-item'))}></sb-docs-table>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import avatar from '../../img/avatar.jpg';
import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-avatar');
const argTypes = getArgTypes(compData);

<Meta title="Avatar/Avatar Tooltip" component="zen-avatar-tooltip" argTypes={argTypes} />

export const StoryWithControls = args =>
  html/*html*/ ` <zen-avatar-tooltip ...="${spreadArgs(args)}"></zen-avatar-tooltip>`;

# Avatar Tooltip

Avatar tooltip component

## Component previews

<br />

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-avatar-tooltip'))}></docs-table>

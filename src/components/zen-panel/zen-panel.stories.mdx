import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-panel');
const argTypes = getArgTypes(compData);

<Meta title="Containers/Panel" component="zen-panel" argTypes={argTypes} />

export const StoryWithControls = args => {
  return html/*html*/ ` <sb-zen-panel ...="${spreadArgs(args, argTypes)}"> Content </sb-zen-panel> `;
};

export const StoryText = () => {
  return html/*html*/ `
    <div>
      <sb-zen-panel header="Panel title 1"> Content </sb-zen-panel>
    </div>
  `;
};

export const StoryDivs = () => {
  return html/*html*/ `
    <div>
      <sb-zen-panel header="Panel title 2">
        <div style="width: 4rem; height: 4rem; background: lightgreen;">Content but in a div</div>
      </sb-zen-panel>
    </div>
  `;
};

export const StoryNoArrow = () => {
  return html/*html*/ `
    <div>
      <sb-zen-panel header="Panel title 3" arrow="false"> Content </sb-zen-panel>
    </div>
  `;
};

# Panel

Expand and collapse a content area.

## Component previews

<br />

### 1. Panel with text

<Canvas>
  <Story name="text">{StoryText.bind({})}</Story>
</Canvas>

### 2. Panel with divs

<Canvas>
  <Story name="divs">{StoryDivs.bind({})}</Story>
</Canvas>

### 3. Panel without arrow

<Canvas>
  <Story name="no-arrow">{StoryNoArrow.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-panel'))}></docs-table>

import { html } from 'lit-html';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { logEvents } from '../../../.storybook/helpers/log-events';
import { getArgTypesAndArgs, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
const { argTypes } = getArgTypesAndArgs('zen-tabs');

<Meta title="In progress/Tabs/Tabs" component="zen-tabs" argTypes={argTypes} />

export const StoryWithControls = args => {
  return html/*html*/ `
    <sb-zen-tabs id="controls" ...="${spreadArgs(args, argTypes)}">
      <sb-zen-tab name="tab1"> Tab1 </sb-zen-tab>
      <sb-zen-tab name="tab2"> Tab2 </sb-zen-tab>
      <sb-zen-tab name="tab3"> Tab3 </sb-zen-tab>
      <sb-zen-tab name="tab4"> Tab4 </sb-zen-tab>
      <sb-zen-tab name="tab5"> Tab5 </sb-zen-tab>
    </sb-zen-tabs>
    ${logEvents('#controls', getComponentData('zen-tabs'))}
  `;
};

# Zen Tabs

A `zen-tabs` component to be used with `zen-tab` fragment.

<br />

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<sb-docs-table docs={JSON.stringify(getComponentData('zen-tabs'))}></sb-docs-table>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-label');
const argTypes = getArgTypes(compData);

<Meta title="Forms/一Fragments/Label" component="zen-label" argTypes={argTypes} />

export const StoryWithControls = args => html/*html*/ ` <zen-label ...="${spreadArgs(args)}"></zen-label> `;

export const StorySimple = () =>
  html/*html*/ `
    <zen-form-group>
      <zen-label label="Username" required></zen-label>
      <zen-input></zen-input>
    </zen-form-group>
  `;

# Label

Simple label text component that can be used along form components like input, checkbox and radio

## Component previews

<br />

### 1. Input with Label

<Canvas>
  <Story name="StorySimple">{StorySimple.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-label'))}></docs-table>

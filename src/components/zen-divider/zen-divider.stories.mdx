import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-divider');
const argTypes = getArgTypes(compData);

<Meta title="Layout/Divider" component="zen-divider" argTypes={argTypes} />

export const StoryWithControls = args => {
  return html/*html*/ ` <sb-zen-divider ...="${spreadArgs(args, argTypes)}"></sb-zen-divider> `;
};

export const StoryText = () => {
  return html/*html*/ `
    <div>
      <zen-text>Faucibus non class pretium laoreet lacus placerat maecenas litora iaculis.</zen-text>
      <sb-zen-divider></sb-zen-divider>
      <zen-text>Dis imperdiet eu natoque et scelerisque nisi</zen-text>
    </div>
  `;
};

# Divider

Separate content.

## Component previews

<br />

### 1. Simple divider example

<Canvas>
  <Story name="text">{StoryText.bind({})}</Story>
</Canvas>

import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';

import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-divider');
const argTypes = getArgTypes(compData);

<Meta title="Layout/Divider" component="zen-divider" argTypes={argTypes} />

export const StoryWithControls = args => {
  return html/*html*/ ` <zen-divider ...="${spreadArgs(args, argTypes)}"></zen-divider> `;
};

export const StoryText = () => {
  return html/*html*/ `
    <div>
      Content A
      <zen-divider></zen-divider>
      Content B
    </div>
  `;
};

export const StoryDivs = () => {
  return html/*html*/ `
    <div>
      <div style="width: 50%; height: 2rem; background: tomato;"></div>
      <zen-divider></zen-divider>
      <div style="width: 50%; height: 2rem; background: lightblue;"></div>
    </div>
  `;
};

# Divider

Separate content.

## Component previews

<br />

### 1. Divide text

<Canvas>
  <Story name="text">{StoryText.bind({})}</Story>
</Canvas>

### 2. Divide divs

<Canvas>
  <Story name="divs">{StoryDivs.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-divider'))}></docs-table>

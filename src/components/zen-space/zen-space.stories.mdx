import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
const css = /*css*/ `
  .row {
    display: flex;
    flex-wrap: wrap;
  }
  .col {
    margin: 1rem;
    flex: 1 0 0;
  }
  zen-space {
    min-width: 300px;
    max-width: 600px;
    border: 1px dashed #adb5bd;
    border-radius: 2px;
  }
  zen-space.with-controls,
  zen-space[vertical] {
    min-width: 0;
    min-height: 250px;
  }
  .story {
    margin: 0.5rem;
    flex: 1 0 0;
    min-width: 300px;
  }
  .story zen-space {
    min-width: 0;
  }
  .item {
    width: auto;
    background-color: #82bcf1;
    color: #fff;
    border-radius: 3px;
    padding: 0.2rem 1rem;
    font-size: 0.875rem;
  }
  .item.tall {
    background-color: #959ea8;
    padding-top: 0.5rem;
    padding-bottom: 1.5rem;
  }
  .item.slim {
    background-color: #0078cd;
    padding-left: 0.25rem;
    padding-right: 0.25rem;
  }
  .item.short {
    padding-top:0;
    padding-bottom:0;
    background-color: #abd2f5;
  }
  p.info {
    font-size: 0.875rem;
    margin-top: 1rem;
    margin-bottom: 0.5rem;
  }
`

import data from '../../../stencilDocs.json';
const compData = data.components.find(n => n.tag === 'zen-space');
const argTypes = getArgTypes(compData);

<Meta title="Layout/Space" component="zen-space" argTypes={argTypes} />

export const StorySpacing = () => {
  const column1 = ['none', 'xs', 'sm'];
  const column2 = ['md', 'lg', 'xl'];
  return html/*html*/`
    <div class="row">
      <div class="col">
        ${column1.map(size => html/*html*/`
            <p class="info">spacing: <b>${size}</b></p>
            <zen-space spacing="${size}" padding="sm">
              <div class="item">Item 1</div>
              <div class="item">Item 2</div>
              <div class="item">Item 3</div>
            </zen-space>
        `)}
      </div>
      <div class="col">
        ${column2.map(size => html/*html*/`
            <p class="info">spacing: <b>${size}</b></p>
            <zen-space spacing="${size}" padding="sm">
              <div class="item">Item 1</div>
              <div class="item">Item 2</div>
              <div class="item">Item 3</div>
            </zen-space>
        `)}
      </div>
    </div>
  `;
};

export const StoryRowAlign = () => {
  const aligns = ['start', 'end', 'center', 'baseline', 'stretch'];
  return html/*html*/`
    <div class="row">
      <div class="col">
        ${aligns.map(align => html/*html*/`
            <p class="info">vertical-align: <b>${align}</b></p>
            <zen-space vertical-align="${align}" horizontal-align="center" padding="sm">
              <div class="item">Item</div>
              <div class="item tall slim">Tall</div>
              <div class="item short">Short</div>
              <div class="item tall">Giant</div>
            </zen-space>
        `)}
      </div>
      <div class="col">
        ${aligns.map(align => html/*html*/`
            <p class="info">horizontal-align: <b>${align}</b></p>
            <zen-space horizontal-align="${align}" padding="sm">
              <div class="item">Item</div>
              <div class="item tall slim">Tall</div>
              <div class="item short">Short</div>
              <div class="item tall">Giant</div>
            </zen-space>
        `)}
      </div>
    </div>
  `;
};

export const StoryColumnAlign = () => {
  const aligns = ['start', 'end', 'center', 'baseline', 'stretch'];
  return html/*html*/`
    <div class="row">
      ${aligns.map(align => html/*html*/`
        <div class="col tall">
          <p class="info">vertical-align:<br/><b>${align}</b></p>
          <zen-space vertical vertical-align="${align}" horizontal-align="center" padding="sm">
            <div class="item">Item</div>
            <div class="item tall slim">Tall</div>
            <div class="item short">Short</div>
            <div class="item tall">Giant</div>
          </zen-space>
        </div>
      `)}
    </div>
    <div class="row">
      ${aligns.map(align => html/*html*/`
        <div class="col tall">
          <p class="info">horizontal-align:<br/><b>${align}</b></p>
          <zen-space vertical horizontal-align="${align}" padding="sm">
            <div class="item">Item</div>
            <div class="item tall slim">Tall</div>
            <div class="item short">Short</div>
            <div class="item tall">Giant</div>
          </zen-space>
        </div>
      `)}
    </div>
  `;
};

export const StoryPadding = () => {
  const values = ['lg', 'sm lg', 'sm sm xl lg'];
  return html/*html*/`
    <div class="row">
      ${values.map(padding => html/*html*/`
        <div class="story">
          <p class="info">padding: <b>"${padding}"</b></p>
          <zen-space padding="${padding}" horizontal-align="stretch" vertical-align="stretch">
              <div class="item">Item 1</div>
              <div class="item">Item 2</div>
              <div class="item">Item 3</div>
            </zen-space>
        </div>
      `)}
    </div>
  `;
};

export const StoryWithControls = args => {
  return html/*html*/ `
    <style>${css}</style>
    <zen-space class="with-controls" ...="${spreadArgs(args)}">
      <div class="item">Item</div>
      <div class="item tall slim">Tall</div>
      <div class="item short">Short</div>
      <div class="item tall">Giant</div>
    </zen-space>
  `;
};

# Space

Spacing component for quick and standradized elements layouting.<br/>
Under the hood it uses <code>flex-box</code>.

## Component previews

<br />

### 1. Items spacing

- Use different values for prop `spacing` to determine space between the items

<Canvas>
  <Story
    name="sizes"
  >
    {StorySpacing.bind({})}
  </Story>
</Canvas>

### 2. Align content of row

- Use props `vertical-align` and `horizontal-align` to align items

<Canvas>
  <Story
    name="aligns"
  >
    {StoryRowAlign.bind({})}
  </Story>
</Canvas>

### 3. Align content of column

- To align items vertically (column) set attr `vertical`
- Use props `vertical-align` and `horizontal-align` to align items

<Canvas>
  <Story
    name="alignsColumn"
  >
    {StoryColumnAlign.bind({})}
  </Story>
</Canvas>

### 4. Inner padding - Shorthand Property

- To shorten the code, it is possible to specify all the padding in one property.
- Same as you can do with normal `padding` and `margin` css properties.

<Canvas>
  <Story
    name="paddings"
  >
    {StoryPadding.bind({})}
  </Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-space'))}></docs-table>

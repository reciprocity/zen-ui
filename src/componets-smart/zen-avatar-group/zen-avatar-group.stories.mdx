import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { html } from 'lit-html';
import { getArgTypes, getDefaultArgs, getComponentData, spreadArgs } from '../../../.storybook/helpers/argTypes';
import data from '../../../stencilDocs.json';
import { waitForElement } from '../../../.storybook/helpers/utils';
const compData = data.components.find(n => n.tag === 'zen-avatar-group');
const argTypes = getArgTypes(compData);

<Meta title="Smart/Avatar Group" component="zen-avatar-group" argTypes={argTypes} />

export const users = [
  {
    userName: 'Mike Anderson',
    email: 'mike.anderson@reciprocitylabs.com',
    imageUrl: '',
    color: '#00528C',
    background: '#D5E9FA',
  },
  {
    userName: 'Frank Sinatra',
    email: 'frank.sinatra@reciprocitylabs.com',
    imageUrl: '',
    color: '#643A6B',
    background: '#FADBFF',
  },
  {
    userName: 'Alex Godunov',
    email: 'alex.godunov@reciprocitylabs.com',
    imageUrl: '',
    color: '#A26900',
    background: '#FFEED2',
  },
  {
    userName: 'Jill Franklin',
    email: 'jill.franklin@reciprocitylabs.com',
    imageUrl: '',
    color: '#1B422E',
    background: '#D8F2E3',
  },
  {
    userName: 'Mark Gray',
    email: 'mark.gray@reciprocitylabs.com',
    imageUrl: '',
    color: '#00528C',
    background: '#D5E9FA',
  },
  {
    userName: 'Greg Downson',
    email: 'greg.downson@reciprocitylabs.com',
    imageUrl: '',
    color: '#643A6B',
    background: '#FADBFF',
  },
  {
    userName: 'Niki General',
    email: 'niki.general@reciprocitylabs.com',
    imageUrl: '',
    color: '#A26900',
    background: '#FFEED2',
  },
  {
    userName: 'Alex Dark',
    email: 'alex.dark@reciprocitylabs.com',
    imageUrl: '',
    color: '#1B422E',
    background: '#D8F2E3',
  },
];

export const StoryWithControls = args => {
  waitForElement('#avatar-group-control', element => {
    element.users = users;
  });
  return html/*html*/ ` <div
    style="font-size:12px; display: flex; justify-content: space-evenly; margin-top:230px; height: 300px;"
  >
    <div style="display: flex; flex-direction: column; align-items: center;">
      <zen-avatar-group id="avatar-group-control" ...="${spreadArgs(args)}" />
    </div>
  </div>`;
};

export const StoryAvatarGroup = () => {
  waitForElement('#avatar-group', element => {
    element.users = users;
  });
  return html/*html*/ `
    <div style="font-size:12px; display: flex; justify-content: space-evenly; margin-top:230px; height: 300px;">
      <div style="display: flex; flex-direction: column; align-items: center;">
        <zen-avatar-group id="avatar-group" />
      </div>
    </div>
  `;
};

# Profile Icons Group

Avatar Group component

## Component previews

<br />

### 1. Avatar group

<Canvas>
  <Story name="StoryAvatarVariants">{StoryAvatarGroup.bind({})}</Story>
</Canvas>

## Properties

<Canvas>
  <Story
    name="default"
    args={{
      ...getDefaultArgs(argTypes),
      users: { users },
    }}
  >
    {StoryWithControls.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />

<docs-table docs={JSON.stringify(getComponentData('zen-avatar-group'))}></docs-table>
